[project]
name = "repl-nix-pixelartgenai"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "diffusers>=0.31.0",
    "streamlit>=1.40.1",
    "torch>=2.5.1",
]
